<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    android:id="@+id/infowindow"
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:orientation="horizontal"
    android:layout_width="match_parent"
    android:layout_height="@dimen/listRowHeight">

    <ImageView
        android:id="@+id/if_icon"
        android:paddingStart="5dp"
        android:paddingEnd="5dp"
        android:layout_width="50sp"
        android:layout_height="match_parent"
        app:srcCompat="@drawable/burger"/>

    <RelativeLayout
        android:id="@+id/if_content"
        android:paddingStart="10dp"
        android:paddingEnd="10dp"
        android:layout_width="match_parent"
        android:layout_height="@dimen/listRowHeight">

        <LinearLayout
            android:id="@+id/if_title"
            android:paddingStart="10dp"
            android:paddingEnd="5dp"
            android:paddingTop="5dp"
            android:layout_width="match_parent"
            android:layout_height="wrap_content">
            <TextView
                android:id="@+id/if_name"

                android:textSize="20sp"
                android:text="Coco's Bubble Tea"

                android:layout_width="0dp"
                android:layout_weight="1"
                android:layout_height="match_parent" />

            <TextView
                android:id="@+id/if_price"
                android:text="$$$"
                android:paddingStart="5dp"
                android:textSize="20sp"
                android:layout_width="wrap_content"
                android:layout_height="match_parent" />
        </LinearLayout>

        <TextView
            android:id="@+id/if_distance"

            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_below="@+id/if_title"

            android:paddingStart="10dp"
            android:paddingEnd="10dp"
            android:paddingTop="5dp"

            android:text="200 M" />
    </RelativeLayout>

</LinearLayout>